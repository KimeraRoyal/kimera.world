---
layout: default
---
<header class="page-header">
    <h1>
        {{ data.title }}
        {% if data.feed %}
            <a class="rss-icon" href="{{ data.feed }}"><img src="/images/icon_rss.png" /></a>
        {% endif %}
    </h1>
</header>

{{ content }}
