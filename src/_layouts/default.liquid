<!doctype html>
<html lang="{{ site.locale }}">
  <head>
    {% if data.titlebar %}
      {% assign title = data.titlebar %}
    {% else %}
      {% assign title = data.title %}
    {% endif %}
    {% render "head", metadata: site.metadata, title: title %}
  </head>
  <body class="{{ data.layout }} {{ data.page_class }}">
    {% render "navbar", metadata: site.metadata, resource: resource %}

    <main class="content-box">
      {% if data.icon %}
        {% assign icon = data.icon %}
      {% else %}
        {% assign icon = nil %}
      {% endif %}
      {% render "titlebar", label: title, icon: icon %}

      <div class="content">
        {{ content }}
      </div> <!-- content -->
    </main> <!-- content-box -->

    {% render "footer", metadata: site.metadata %}
  </body>
</html>
